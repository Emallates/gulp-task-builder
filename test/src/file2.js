'use strict'

var file2 = true

var link = function(height = 50, color = 'red', url = 'http://azat.co') {}
let first='First'
let last='Last'

let fun = (a1,a2,a3)=>{ return a1+a2+a3 }

var name = `Your name is ${first} ${last}.`

fun(2,3,5)
console.log(name)
